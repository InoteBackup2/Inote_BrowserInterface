export const environment = {
    /** Frontend host address */
    FRONTEND_HOST: "localhost",
    /** Frontend port */
    FRONTEND_PORT: 8080,
    /** Toaster timeout (seconds) */
    TOASTER_TIMEOUT: 5000
} as const;
